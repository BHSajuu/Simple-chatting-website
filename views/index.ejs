<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsapp</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
</head>
<body>
    <h2>All chats</h2>
    
    <form method="GET" action="/chats/new">
        <button>Add new message</button>
    </form>
    <br>
    <br>
<% for(let chat of chats){ %>
    <div class="chat">
        
        <p class="nam"><b>From :</b><%= chat.from %></p>
        <div class="msg">
            <form  method="GET" action="/chats/<%= chat._id %>/edit"><button style="background-color: #9ACD32;"><span class="material-symbols-outlined">edit</span></button></form>
            <p><%= chat.message %></p>
            <form method="POST" action="/delete/<%= chat._id %>/chat?_method=DELETE"><button>delete</button></form>
        </div>
        <p><b>Receive by :</b><%= chat.to %></p>
        <br>
        <p><%=chat.created_at.toString().split(" ")[4] %></p>
        <p><%= chat.created_at.toString().split(" ").slice(0,1) %></p>
        <p><%= chat.created_at.toString().split(" ").slice(1,4).join(" ") %></p>
        <hr>
    </div>
    <br>
<% } %>
</body>
</html>
